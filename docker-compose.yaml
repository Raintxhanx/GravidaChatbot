version: "3.9"

services:
  app:
    build: .
    container_name: pregnancy_app
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://ajiandrain:RaihanSecret1309@db:5432/pregnancy_guide
    ports:
      - "8021:8021"
    networks:
      - pg_network

networks:
  pg_network:
    external: true